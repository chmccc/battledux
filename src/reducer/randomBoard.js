const createHitMissBoard = (n = 8) => new Array(n).fill(new Array(n).fill('W'));
const createDucksBoard = (n = 8, numGeese = 1, numDucks = 1, numDucklings = 1)

const board = {
  hits: createHitMissBoard(),
  
}